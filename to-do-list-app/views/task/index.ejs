<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TO-DO LIST</title>
</head>
<body>
    <h1>List</h1>
    <ul>
        <% for(let task of tasks) {%>
        <li> <a href="/toDoList/<%= task._id %>"><%= task.name %> </a>
            - <%= task.date.getUTCDate() %> 
            / <%= task.date.getUTCMonth() + 1 %> 
            / <%= task.date.getUTCFullYear() %> 
            - <%= task.note %>
        </li>
        <% } %>
    </ul>
    <a href="/toDoList/add">Add a new task</a>
</body>
</html>


<!-- date_obj.getUTCFullYear()
date_obj.getUTCMonth()
date_obj.getUTCDate()
date_obj.getUTCHours()
date_obj.getUTCMinutes()
date_obj.getUTCSeconds() -->